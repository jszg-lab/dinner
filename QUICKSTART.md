# 聚餐投票小程序 - 快速启动指南

## 环境要求

- Node.js >= 16.0.0
- npm >= 8.0.0 或 pnpm >= 7.0.0

## 安装步骤

### 1. 安装依赖

```bash
cd dinner-party-voting
npm install
```

### 2. 启动开发服务器

```bash
npm run dev
```

访问 http://localhost:5173

### 3. 构建生产版本

```bash
npm run build
```

构建产物将输出到 `dist` 目录。

## 测试账号

| 角色 | 昵称 | 密码 | 说明 |
|------|------|------|------|
| 管理员 | 管理员 | admin123 | 拥有所有权限 |
| 组织者 | （需管理员设置） | - | 发布投票、上传收款码 |
| 成员 | （管理员添加） | - | 参与投票、查看结算 |

## 功能概览

### 管理员功能
- 管理餐厅数据（增删查改）
- 批量导入餐厅（JSON文件）
- 用户管理（添加/批量添加/设置角色/分配部门）
- 部门管理（创建/删除部门）
- 管理本部门投票

### 组织者功能
- 发布聚餐投票
- 设置投票时间和选项
- 设置聚餐金额和报销额度
- 上传收款码
- 结束投票
- 归档投票并导出

### 普通成员功能
- 登录系统
- 确认是否参与聚餐
- 参与投票选择餐厅
- 实时查看投票结果
- 查看结算信息和收款码

## 核心功能说明

### 1. 部门隔离
- 每个管理员管理一个部门
- 不同部门之间的投票数据互不干扰
- 用户可以被分配到指定部门

### 2. 投票流程
1. 组织者发布投票
2. 成员登录后确认是否参与
3. 参与者选择意向餐厅
4. 投票结束后组织者设置金额
5. 系统自动计算每人应付金额
6. 组织者上传收款码

### 3. 归档功能
- 投票结束后可归档
- 归档包含完整聚餐信息：
  - 餐厅列表及投票结果
  - 参与人员名单
  - 不参与人员名单
  - 结算金额明细
- 支持导出为文本文件

### 4. 批量添加用户
格式：每行一个用户，`昵称,密码`
```
张三,abc123
李四,xyz789
王五
```
密码可选，默认为 `123456`

## JSON 导入格式

```json
{
  "meta": {
    "version": "1.0",
    "data_source": "数据来源"
  },
  "restaurants": [
    {
      "name": "餐厅名称",
      "cuisine_type": "菜系",
      "avg_price": 100,
      "rating": 4.5,
      "address": "地址",
      "phone": "电话",
      "business_hours": "营业时间",
      "has_private_room": true,
      "tags": ["标签1", "标签2"]
    }
  ]
}
```

## 技术栈

- React 18
- Vite 5
- TailwindCSS 3
- React Router 6
- Lucide React
- Recharts

## 目录结构

```
dinner-party-voting/
├── src/
│   ├── components/       # UI组件
│   │   ├── common/        # 通用组件
│   │   ├── auth/          # 认证组件
│   │   ├── restaurant/    # 餐厅组件
│   │   ├── vote/          # 投票组件
│   │   └── settlement/    # 结算组件
│   ├── pages/             # 页面
│   ├── services/          # 业务逻辑
│   ├── context/           # 状态管理
│   ├── utils/             # 工具函数
│   └── data/              # 模拟数据
├── index.html
├── package.json
├── vite.config.js
└── tailwind.config.js
```

## 注意事项

1. 数据存储：所有数据保存在浏览器 LocalStorage 中
2. 清理数据：清除浏览器缓存将丢失所有数据
3. 收款码：图片以 Base64 格式存储，大小限制 5MB
4. 部门隔离：不同部门用户只能看到本部门的投票
5. 昵称唯一：系统保证昵称唯一，作为登录标识
